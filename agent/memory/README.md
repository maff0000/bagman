# agent/memory/

Owns the BAGMAN AI agent's interface to BAGMAN's durable memory fabric
(`memory/`) — how the agent reads and writes institutional memory under
governance, as distinct from the memory content itself.

Empty scaffolding as of CD-1. No implementation exists yet (see `PID.md`
§16).
