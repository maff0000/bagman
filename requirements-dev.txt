# Development/test-only dependencies for BAGMAN.
# Never add runtime application dependencies here — this file is for the
# tooling that runs the test suite (tests/security/ etc.) and CI only.

pytest==8.3.4
# PyYAML moved to requirements.txt by WI-3: it is now imported directly by
# scripts/generate_architecture_memory.py, which is meant to run outside
# test contexts (developer/CI drift-check usage), not only under pytest.
#
# httpx2 - added by CD-3 WI-3 PL-review re-verification: fastapi.testclient.
# TestClient (used by tests/app_api/) requires it transitively via
# starlette.testclient at test-collection time — the WI-3 dispatch that
# introduced fastapi/starlette to requirements.txt never added this
# dev-only test dependency, so `pytest tests/app_api/` failed to even
# collect (`RuntimeError: ... requires the httpx2 package`) before this.
# starlette 1.6.0 (pulled in transitively by fastapi==0.141.1) prefers
# httpx2 over the now-deprecated `httpx`-with-starlette.testclient path.
# Test-only (never imported by application code under app/), so it
# belongs here, not in requirements.txt.
httpx2==2.12.0
#
# requests - moved to requirements.txt by CD-5 WI-3 PL reconciliation
# (2026-09-13): originally declared here (CD-3 WI-4 review) because only
# tests/acceptance/_lib.py imported it. CD-5 WI-3's ai/providers/claude/
# client.py now imports it directly from application code
# (app/api/composition.py's production path constructs a real
# ClaudeClient), so per this file's own header discipline ("never a
# runtime application dependency here"), it no longer belongs in this
# file at all — see requirements.txt for the current pin and rationale.
