# deployment/docker/

Owns Dockerfiles and image build definitions for BAGMAN's own containers
(`bagman-<operational-role>`, e.g. `bagman-api`, `bagman-worker`), per the
Docker-first doctrine in `ARCHITECTURE.md`.

Empty scaffolding as of CD-1. No working application containers exist yet
(see `PID.md` §16).
