# tests/integration/

Integration tests for BAGMAN — end-to-end tests exercising real component
wiring (services, adapters, datastore) rather than mocked boundaries.

Empty scaffolding as of CD-1. No business capability or connectivity
exists yet, so no integration tests exist yet (see `PID.md` §16).
