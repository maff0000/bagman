# agent/tools/

Owns the discrete tools the BAGMAN AI agent may invoke, each scoped to a
specific, governed operation against BAGMAN's own services.

Empty scaffolding as of CD-1. No implementation exists yet (see `PID.md`
§16).
