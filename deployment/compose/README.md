# deployment/compose/

Owns Docker Compose definitions that assemble BAGMAN's containers into a
running stack for local development and eventual deployment.

Empty scaffolding as of CD-1. No working compose stack exists yet (see
`PID.md` §16).
