# agent/bagman/

Owns the BAGMAN AI agent itself — its core reasoning/orchestration layer,
acting only as a governed client of BAGMAN's own services (see
`ARCHITECTURE.md`), never holding broad credentials directly.

Empty scaffolding as of CD-1. No implementation exists yet; no BAGMAN AI
reasoning or autonomous action is authorised at this stage (see `PID.md`
§16).
