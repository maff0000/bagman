# BAGMAN component manifest (PID §7/§19) for agent/.
# Validated by scripts/generate_architecture_memory.py against
# contracts/manifest/bagman.component_manifest.v1.schema.json.

id: BAGMAN.AGENT
version: 3

# ---------------------------------------------------------------------
# ARCHITECTURE CORRECTION AND CLEANUP (2026-09-16, CD-5 Gate-2 closure,
# final delta before merge) — read this before the history below.
#
# The architect ruled BAGMAN's operator intelligence is NOT a
# direct-Anthropic-API tool-calling loop under BAGMAN's own API key —
# it is a bounded, headless Claude Code invocation, owned entirely by
# the sibling component agent/claude_code/ (see that package's own
# component.yaml, BAGMAN.AGENT.CLAUDE_CODE). `POST /internal/operator/chat`
# (app/api/routers/operator.py) calls
# agent.claude_code.orchestrator.handle_operator_message — the ONE,
# sole authoritative operator implementation.
#
# `agent/bagman/` and `agent/tools/` — the original CD-5 WI-3
# implementation this manifest's `responsibility` text below describes
# — were first superseded (2026-09-16, still present but no longer
# reachable from any live HTTP route) and then, once a fresh Auditor
# and the PL both independently confirmed zero remaining live
# dependents anywhere in the repository, REMOVED ENTIRELY as this same
# day's final closure delta (not merely left unreferenced) — the
# architect's own explicit instruction: "removed before merge to
# prevent dual-authority ambiguity." `ai/providers/claude/` (the
# direct Anthropic Messages API adapter these packages depended on)
# was removed in the same delta — see `ai/component.yaml`'s own
# equivalent note.
#
# PRESERVE HISTORY, do not pretend this never existed: the
# `responsibility` text below is WI-3's own original description,
# kept verbatim, of what `agent/bagman/`+`agent/tools/` WERE before
# removal — it is history, not a description of anything present in
# this repository today. `agent/` today owns only the empty CD-1
# placeholder directories (`agent/policies/`, `agent/memory/`) and the
# separately-manifested `agent/claude_code/` — see PID.md §97 and the
# CD-5 evidence file for the full chronological account (architecture
# correction → classification finding → this removal).
# ---------------------------------------------------------------------

responsibility: >
  [REMOVED 2026-09-16 — see the correction/cleanup note above; text
  below is WI-3's own original `responsibility` text for
  agent/bagman/+agent/tools/, preserved verbatim as history. Neither
  directory exists in this repository any more.] Own the BAGMAN AI
  agent's own reasoning/orchestration layer: the Ask BAGMAN
  operator-orchestration loop (agent/bagman/ — system-prompt
  construction, the bounded Claude tool-calling loop, AIInvocation
  lifecycle management, PID §57 audit emission) and the fixed,
  read-only/analyse-only governed tool registry Claude may invoke
  (agent/tools/ — CD-5 PID §34's 8 named tools, mechanically
  fail-closed dispatch per PID §63). This component decided WHICH of
  BAGMAN's own internal APIs/repositories a registered tool could call
  and HOW an Ask BAGMAN conversation was assembled/bounded (PID §53);
  it never instantiated an Anthropic/LiteLLM client itself (that lived
  in ai/providers/*/, consumed here only through the provider-neutral
  ClaudeClientProtocol/BackgroundTaskRunner seams) and never allowed
  Claude to invoke anything outside the fixed tool set or to mutate
  canonical state (PID §17/§56). agent/policies/ and agent/memory/
  remain empty CD-1 placeholders — no autonomous policy engine is
  authorised yet, and Ask BAGMAN's conversation state remains
  in-request-only (PID §64 — no durable chat-memory-fabric integration
  exists).

# Today, `agent/` owns only the empty CD-1 placeholder directories
# (agent/policies/, agent/memory/) — no code. The live operator
# implementation is entirely owned by the separately-manifested
# agent/claude_code/ (BAGMAN.AGENT.CLAUDE_CODE).
owns: []

consumes: []

# No audit events are minted at this level any more — that is entirely
# agent/claude_code/orchestrator.py's job now (see that component's
# own manifest for its produces: list, unchanged from what this
# component used to claim).
produces: []

dependencies: []

external_access: false

prohibited:
  - direct_email_access
  - direct_bank_access
  - direct_xero_access
  - direct_chargebee_access
