# deployment/environments/

Owns environment-specific (non-secret) runtime configuration for BAGMAN
deployments — Layer 2 of the configuration contract described in
`ARCHITECTURE.md` and `config/README.md`.

Empty scaffolding as of CD-1. No implementation exists yet (see `PID.md`
§16).
