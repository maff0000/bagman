# ui/

Owns the BAGMAN GUI — the human-facing client, acting only as a governed
client of BAGMAN's own services (see `ARCHITECTURE.md`), never holding
broad credentials directly.

Empty scaffolding as of CD-1. No implementation exists yet; no production
GUI or production authentication is authorised at this stage (see
`PID.md` §16).
